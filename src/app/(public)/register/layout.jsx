export const metadata = {
  title: "Register | BookWorm",
  description: "Connect with BookWorm to access book details.",
};

export default function RegisterLayout({ children }) {
  return children;
}
