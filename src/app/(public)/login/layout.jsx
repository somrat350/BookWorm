export const metadata = {
  title: "Login | BookWorm",
  description: "Login to your BookWorm account to manage your library.",
};

export default function LoginLayout({ children }) {
  return children;
}
