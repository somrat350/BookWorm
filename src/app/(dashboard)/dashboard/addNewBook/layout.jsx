const AddNewBookLayout = ({ children }) => {
  return children;
};

export default AddNewBookLayout;

export const metadata = {
  title: "Add New Book | BookWorm",
  description:
    "Administrator panel to add new books to the BookWorm library. Input details like ISBN, Title, Author, and Genres.",
  robots: {
    index: false,
    follow: false,
  },
};
