const ProfileLayout = ({ children }) => {
  return children;
};

export default ProfileLayout;

export const metadata = {
  title: "Profile | BookWorm",
  description:
    "Manage your BookWorm profile, reading history, and account settings.",
};
