export const metadata = {
  title: "Manage Books | BookWorm",
  description:
    "Administrator panel to edit, update, or remove books from the BookWorm library.",
  robots: { index: false, follow: false },
};

const ManageBooksLayout = ({ children }) => {
  return children;
};

export default ManageBooksLayout;
